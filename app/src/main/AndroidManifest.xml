<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jinyuanyuan.bw.com.androidprojects">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/aaa"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.RgisterActivity" />
        <activity
            android:name=".activity.HomeActivity"
            android:windowSoftInputMode="adjustPan"
            android:launchMode="singleTop"/>
        <activity android:name=".activity.MySourceActivity" />
        <activity android:name=".activity.SeacherActivity" />
        <activity android:name=".activity.GoodsActivity" />
        <activity android:name=".activity.MyWalletActivity" />
        <activity android:name=".activity.MyAddressActivity" />
        <activity android:name=".activity.AddAddressActivity" />
        <activity android:name=".activity.UpdateAddressActivity" />
        <activity android:name=".activity.DetailsActivity" />
        <activity android:name=".activity.CheckoutActivity" />
        <activity android:name=".activity.MyFootActivity"></activity>
    </application>

</manifest>